---
interface Props {
	title: string;
}

const { title } = Astro.props;
const { pathname } = Astro.url;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Every etching known, with descriptive data and images." />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link href="https://fonts.googleapis.com/css?family=Cormorant+Garamond:700i|Fira+Sans" rel="stylesheet">	
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<div class="flex justify-center">
			<div class="w-5/6 lg:w-3/5">
				<h1 class="text-5xl pt-16 pb-4 text-center">
					<span class="font-bold">Samuel Colman:</span><br />
					<span class="text-4xl">A Catalogue of the Artist&rsquo;s Etchings</span>
				</h1>

				<div class="banner text-xl text-center py-8 font-bold">
					This site is under construction.
				</div>

				<ul id="main-menu" class="grid grid-cols-3 text-center pt-4 justify-center items-center md:ml-18 md:mr-18 md:text-xl">
					<li><a class={pathname === "/" ? "active" : ""} href="/">The Catalogue</a></li>
					<li><a class={pathname === "/colman/" ? "active" : ""} href="/colman/">Artist Biography</a></li>
					<li><a class={pathname === "/faq/" ? "active" : ""} href="/faq/">FAQ</a></li>
				</ul>
			
				<main>
					<slot />
				</main>
			</div>
		</div>
	</body>
</html>

<style is:global>
	:root {
		--bg: #f7e3c1;
		--missing: #eed6ac;
		--links: #b85429;
		--text: #403025;
	}

	body {
		background-color: var(--bg);
		color: var(--text);
	}

	main {
		padding-top: 20px;
		padding-bottom: 40px;
	}

	h1, h2 {
		@apply font-serif;
	}

	h2 {
		font-size: 1.5rem;
		margin-top: 2rem;
	}

	h3 {
		font-size: 1.2rem;
		margin-top: 1rem;
		font-weight: bold					;
	}
	
    a {
        color: var(--links);
    }

	ul li {
		@apply font-sans;
	}

	#main-menu li a.active {
		font-weight: bold;
	}

    p {
		@apply font-sans;
        padding-bottom: 0.5rem;
    }
</style>